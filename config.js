const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "db4free.net",
      user: "marktsai",
      password: "lidemy0228",
      database: "gym_att",
    },
    listPerPage: 10,
  };
  module.exports = config;